# =============================================
# RAG AI 어시스턴트 - 패키지 목록
# Python 3.12 / Windows / 폐쇄망 환경
# 설치: pip install -r requirements.txt
# 폐쇄망: pip install --no-index --find-links=./wheels -r requirements.txt
# =============================================

# ── 핵심 프레임워크 ─────────────────────────────────────────────────
langchain==0.3.14
langchain-core==0.3.29
langchain-community==0.3.14
langchain-ollama==0.2.3
langchain-huggingface==0.1.2
langgraph==0.2.61

# ── LLM 엔진 (Ollama 클라이언트) ────────────────────────────────────
ollama==0.4.4

# ── 임베딩 / 리랭커 ─────────────────────────────────────────────────
sentence-transformers==3.3.1
transformers==4.47.1
torch==2.5.1          # CPU 버전: torch==2.5.1+cpu (폐쇄망은 whl 직접 설치)
huggingface-hub==0.27.0

# ── 벡터 DB ─────────────────────────────────────────────────────────
qdrant-client==1.12.1

# ── 문서 처리 ──────────────────────────────────────────────────────
pypdf==5.1.0          # PDF 로딩
docx2txt==0.8         # DOCX 로딩
unstructured==0.16.11 # 범용 문서 파서 (선택)

# ── MCP (Model Context Protocol) ─────────────────────────────────
mcp==1.3.0

# ── Web UI ──────────────────────────────────────────────────────────
streamlit==1.41.1

# ── 환경설정 ────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── 유틸리티 ────────────────────────────────────────────────────────
numpy==1.26.4
pydantic==2.10.4
httpx==0.28.1
aiohttp==3.11.11
