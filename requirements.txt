# =============================================
# RAG AI 어시스턴트 - 패키지 목록
# Python 3.12 / Windows / 폐쇄망 환경
# 설치: pip install -r requirements.txt
# 다운로드: pip download -r requirements.txt -d wheels
# 폐쇄망: pip install --no-index --find-links=./wheels -r requirements.txt
# =============================================


# ──────────────────────────────────────────
# 1. LangChain 생태계  (반드시 같은 0.3.x 라인)
# ──────────────────────────────────────────
langchain>=0.3.14,<0.4.0
langchain-core>=0.3.29,<0.4.0        # 절대 == 고정 금지 → 충돌 원인
langchain-community>=0.3.14,<0.4.0
langchain-text-splitters>=0.3.4,<0.4.0

# ──────────────────────────────────────────
# 2. LangGraph  (0.2.x 라인이 langchain 0.3.x 호환)
# ──────────────────────────────────────────
langgraph>=0.2.66,<0.3.0
langgraph-checkpoint>=2.0.10,<3.0.0

# ──────────────────────────────────────────
# 3. Ollama 연동  (llama3.1:8b 엔진)
# ──────────────────────────────────────────
langchain-ollama>=0.2.2,<0.3.0
ollama>=0.4.0

# ──────────────────────────────────────────
# 4. 임베딩 / 리랭커  (BGE-M3, BGE-Reranker)
#    → 허깅페이스 오프라인 사용 (로컬 캐시 필요)
# ──────────────────────────────────────────
langchain-huggingface>=0.1.0,<0.2.0  # ★ HuggingFaceEmbeddings 등 LangChain↔HF 브릿지
                                      #   (langchain-community의 HF기능이 이 패키지로 분리됨)
FlagEmbedding>=1.2.11                 # BGE-M3 + BGE-Reranker 통합 패키지
sentence-transformers=5.3.0
transformers>=4.40.0,<5.0.0
torch>=2.2.0                          # CPU 전용이면 torch --index-url 옵션 사용
tokenizers>=0.19.0
huggingface-hub>=0.23.0               # 모델 로컬 캐시 경로 제어 (HF_HOME 환경변수 연동)

# ──────────────────────────────────────────
# 5. 벡터 DB  (Qdrant 로컬 모드)
# ──────────────────────────────────────────
qdrant-client>=1.9.0,<2.0.0
langchain-qdrant>=0.2.0,<0.3.0

# ──────────────────────────────────────────
# 6. MCP (Model Context Protocol)
# ──────────────────────────────────────────
mcp>=1.6.0                            # MCP Client / Server SDK
langchain-mcp-adapters>=0.1.0         # LangChain ↔ MCP 브릿지

# ──────────────────────────────────────────
# 7. A2A (Agent-to-Agent Protocol)
# ──────────────────────────────────────────
a2a-sdk>=0.2.0                        # Google A2A 공식 SDK

# ──────────────────────────────────────────
# 8. Web UI  (Streamlit)
# ──────────────────────────────────────────
streamlit>=1.40.0,<2.0.0

# ──────────────────────────────────────────
# 9. DB 연동 (Text-to-SQL)
# ──────────────────────────────────────────
sqlalchemy>=2.0.0,<3.0.0
pymysql>=1.1.0                        # MySQL 사용 시
psycopg2-binary>=2.9.9               # PostgreSQL 사용 시

# ──────────────────────────────────────────
# 10. 유틸리티
# ──────────────────────────────────────────
python-dotenv>=1.0.0                  # .env 파일 로드
pydantic>=2.7.0,<3.0.0               # langchain-core와 동일 라인
pydantic-settings>=2.3.0
tiktoken>=0.7.0                       # 토큰 계산
numpy>=1.26.0,<2.0.0                 # torch 호환을 위해 <2.0 권장
pypdf>=4.0.0                          # PDF 문서 파싱
python-docx>=1.1.0                    # Word 문서 파싱
openpyxl>=3.1.0                       # Excel 파싱
unstructured>=0.14.0                  # 다양한 문서 형식 파싱
httpx>=0.27.0                         # 비동기 HTTP
aiofiles>=23.2.1                      # 비동기 파일 I/O
loguru>=0.7.0                         # 구조화된 로깅
